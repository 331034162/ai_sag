"""
表格识别配置
===========
集中管理表格识别相关的所有可配置常量。
"""

# ============================================================
# 总开关
# ============================================================

ENABLE_TABLE_RECOGNITION = True

# ============================================================
# 后端选择
# ============================================================
# 可选值：
#   "paddle"   — PaddleOCR PP-Structure（精度高但单元格可能错位）
#   "visual"   — 基于几何线条分析（适合有线框的表格）
#   "position" — 基于 OCR 块位置聚类（无需结构模型，推荐）
# ============================================================

TABLE_RECOGNITION_BACKEND = "position"

# ============================================================
# 位置聚类参数
# ============================================================

# 行聚类阈值系数（相对于块高度中位数）
TABLE_ROW_CLUSTER_RATIO = 0.5

# 列检测容差（像素），左边缘 x 坐标差值 < 此值视为同列
TABLE_COL_TOLERANCE_PX = 15

# 表格最少行数（低于此值的行序列不视为表格）
TABLE_MIN_ROWS = 3

# 表格最少列数（低于此值的行序列不视为表格）
TABLE_MIN_COLS = 2
