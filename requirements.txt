# ============================================================
# ai-llamaindex 项目依赖
# ============================================================
# 安装方式：
#   CPU 环境：pip install -r requirements.txt
#   GPU 环境：pip install -r requirements.txt -r requirements-gpu.txt
# ============================================================

# ------------------------------------------------------------
# OCR 引擎（二选一或都装）
# ------------------------------------------------------------
paddleocr>=3.6.0              # PaddleOCR — 精度高，GPU 可加速，CPU 较慢
rapidocr-onnxruntime>=1.4.0   # RapidOCR — 基于 ONNX Runtime，CPU 速度快、轻量

# ------------------------------------------------------------
# 图像处理
# ------------------------------------------------------------
opencv-contrib-python>=4.10.0 # cv2（含 inpaint 等扩展模块）
numpy>=1.26.0

# ------------------------------------------------------------
# PDF 解析
# ------------------------------------------------------------
PyMuPDF>=1.25.0               # fitz — PDF 读写、文字/图片提取、bbox 查询
pymupdf4llm>=1.0.0            # PDF → Markdown 转换

# ------------------------------------------------------------
# LlamaIndex 框架
# ------------------------------------------------------------
llama-index-core>=0.12.0      # 核心：索引、查询引擎、Agent
llama-index>=0.12.0           # 聚合安装入口
llama-index-llms-openai>=0.3.0            # OpenAI 兼容 LLM（DeepSeek 等）
llama-index-embeddings-huggingface>=0.4.0 # HuggingFace 本地 Embedding
llama-index-vector-stores-chroma>=0.3.0   # ChromaDB 向量存储集成

# ------------------------------------------------------------
# 向量数据库
# ------------------------------------------------------------
chromadb>=1.0.0               # 轻量级本地向量数据库

# ------------------------------------------------------------
# 关系型数据库（SAG 事件-实体超边存储）
# ------------------------------------------------------------
aiomysql>=0.2.0               # MySQL 异步驱动（全链路异步改造）

# ------------------------------------------------------------
# Embedding / 模型推理
# ------------------------------------------------------------
torch>=2.0.0                  # PyTorch — GPU 版见 requirements-gpu.txt
transformers>=4.40.0          # HuggingFace Transformers（Qwen3 Embedding）

# ------------------------------------------------------------
# Word 文档解析
# ------------------------------------------------------------
python-docx>=1.1.0            # .docx 读写
lxml>=5.0.0                   # XML 处理（Word 底层 XML 解析）

# ------------------------------------------------------------
# Excel 文档解析
# ------------------------------------------------------------
openpyxl>=3.1.0               # .xlsx 读写

# ------------------------------------------------------------
# LLM API 客户端
# ------------------------------------------------------------
openai>=1.0.0                 # OpenAI SDK（LLM 对比测试等）

# ------------------------------------------------------------
# 云存储（可选，S3 路径时才加载）
# ------------------------------------------------------------
boto3>=1.34.0                 # AWS S3 文件下载

# ------------------------------------------------------------
# 工具库
# ------------------------------------------------------------
pydantic>=2.0.0               # 数据校验与序列化（API Schema、配置模型）
python-dotenv>=1.0.0          # 从 .env 加载环境变量
loguru>=0.7.2                 # 结构化日志（控制台/文件双输出、异步写入、轮转压缩）

# ------------------------------------------------------------
# Web 服务（SAG API 与前端）
# ------------------------------------------------------------
fastapi>=0.110.0              # Web 框架
uvicorn>=0.29.0               # ASGI 服务器
python-multipart>=0.0.9       # 文件上传支持